/** Re-export brand board from production source — integrity tests stay in sync */
export {
  BRAND_COLORS,
  LEGACY_COLORS,
  BRAND_COPY,
  BRAND_ASSETS,
  REQUIRED_CSS_VARS,
} from '../../src/lib/brand'
